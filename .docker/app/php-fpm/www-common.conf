user = www-data
group = www-data

listen.owner = www-data
listen.group = www-data
listen.mode = 0660

pm = static
pm.max_children = 4
pm.max_requests = 200
pm.status_path = /fpmstatus

clear_env = no

request_terminate_timeout = 600s
